- [ ] F1 Build java-services with Maven (`cd backend/java-services && mvn -B -DskipTests package`) — UNVERIFIED (requires outbound HTTPS to download org.springframework.boot:spring-boot-starter-parent:3.3.4).
- [ ] F2 GET /api/research/visualization-data/:category (`curl -sS http://localhost:3001/api/research/visualization-data/alignment`) — UNVERIFIED (needs PostgreSQL database `houston_ej_ai` with `air_quality` data and Redis cache at `redis://localhost:6379`).
- [ ] F3 GET /api/research/network-topology (`curl -sS http://localhost:3001/api/research/network-topology`) — UNVERIFIED (same PostgreSQL and Redis services as F2 are unavailable).
- [ ] F4 POST /api/research/update-metrics (`curl -sS -H 'Content-Type: application/json' -d '{"metrics":[12,18,27,45,3],"device_id":"houston_sensor_001"}' http://localhost:3001/api/research/update-metrics`) — UNVERIFIED (blocked until Redis and PostgreSQL are reachable).
- [ ] F5 GET /metrics (`curl -sS http://localhost:3001/metrics`) — UNVERIFIED (service start fails without Redis and PostgreSQL backends).
- [ ] F6 GET /live (`curl -sS http://localhost:3001/live`) — UNVERIFIED (server process did not start because dependencies are missing).
- [ ] F7 GET /ready (`curl -sS http://localhost:3001/ready`) — UNVERIFIED (server process did not start because dependencies are missing).
