- [ ] F1 Start Node research server (`cd backend/node-server && node src/server.js`) — UNVERIFIED (requires running Postgres + Redis dependencies; not started in this session).
- [ ] F2 GET /api/research/visualization-data/:category (`curl -sS http://localhost:3001/api/research/visualization-data/alignment`) — UNVERIFIED (server not running; requires seeded database).
- [ ] F3 POST /api/research/update-metrics (`curl -sS -H 'Content-Type: application/json' -d '{"metrics":[30,49,28,65,1],"device_id":"sensor_001"}' http://localhost:3001/api/research/update-metrics`) — UNVERIFIED (server not running; requires database write access).
- [ ] F4 GET /metrics (`curl -sS http://localhost:3001/metrics`) — UNVERIFIED (server not running; dependencies unavailable).
- [ ] F5 GET /ready (`curl -sS http://localhost:3001/ready`) — UNVERIFIED (server not running; Postgres/Redis endpoints unset).
- [ ] F6 Run backend/node-server Jest integration suite (`cd backend/node-server && npm test -- --runTestsByPath test/server.integration.test.js`) — UNVERIFIED (fails: missing optional dependency testcontainers; see evidence/2024-01-01T000000Z_npm_test.log).
- [ ] F7 Build analytics Spring Boot service (`cd backend/java-services && mvn -B -DskipTests package`) — UNVERIFIED (requires Maven dependencies with network access).
- [ ] F8 POST /api/analytics/network-analysis (`curl -sS -H 'Content-Type: application/json' -d '["alignment","safety"]' http://localhost:8080/api/analytics/network-analysis`) — UNVERIFIED (service not running).
